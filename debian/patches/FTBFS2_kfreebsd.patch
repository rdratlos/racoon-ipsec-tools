this patch addresses build problems on kfreebsd
Index: pkg-ipsec-tools/configure
===================================================================
--- pkg-ipsec-tools.orig/configure	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/configure	2014-04-25 23:35:53.695882978 -0700
@@ -11900,6 +11900,9 @@
 
 
 	;;
+*kfreebsd*)
+	LIBS="$LIBS -lresolv"
+	;;
 *darwin*)
 	LIBS="$LIBS -lresolv"
 	;;
@@ -14451,6 +14454,7 @@
 #define INET6
 #endif
 #include <sys/types.h>
+#define __USE_GNU 1	/* GNU/kFreeBSD */
 #include <netinet/in.h>
 int
 main ()
@@ -14617,7 +14621,7 @@
 rm -f conftest*
 
 	;;
-freebsd*|netbsd*)
+freebsd*|kfreebsd*|netbsd*)
 # NetBSD case
 # Same check for FreeBSD
 ac_fn_c_check_member "$LINENO" "struct sadb_x_nat_t_type" "sadb_x_nat_t_type_len" "ac_cv_member_struct_sadb_x_nat_t_type_sadb_x_nat_t_type_len" "
Index: pkg-ipsec-tools/src/libipsec/key_debug.c
===================================================================
--- pkg-ipsec-tools.orig/src/libipsec/key_debug.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/libipsec/key_debug.c	2014-04-25 23:35:53.695882978 -0700
@@ -790,7 +790,7 @@
 		    m->m_pkthdr.len, m->m_pkthdr.rcvif);
 	}
 
-#ifdef __FreeBSD__
+#if defined(__FreeBSD__) || defined(__FreeBSD_kernel__)
 	if (m->m_flags & M_EXT) {
 		printf("  m_ext{ ext_buf:%p ext_free:%p "
 		       "ext_size:%u ext_ref:%p }\n",
Index: pkg-ipsec-tools/src/libipsec/policy_token.c
===================================================================
--- pkg-ipsec-tools.orig/src/libipsec/policy_token.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/libipsec/policy_token.c	2014-04-25 23:35:53.699883002 -0700
@@ -625,7 +625,7 @@
 #include "libpfkey.h"
 
 #if !defined(__NetBSD__) && !defined(__FreeBSD__) && !defined(__linux__)  && \
-!defined(__APPLE__) && !defined(__MACH__)
+!defined(__APPLE__) && !defined(__MACH__) && !defined(__FreeBSD_kernel__)
 #include "y.tab.h"
 #else
 #include "policy_parse.h"
Index: pkg-ipsec-tools/src/libipsec/policy_token.l
===================================================================
--- pkg-ipsec-tools.orig/src/libipsec/policy_token.l	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/libipsec/policy_token.l	2014-04-25 23:35:53.699883002 -0700
@@ -52,7 +52,7 @@
 #include "libpfkey.h"
 
 #if !defined(__NetBSD__) && !defined(__FreeBSD__) && !defined(__linux__)  && \
-!defined(__APPLE__) && !defined(__MACH__)
+!defined(__APPLE__) && !defined(__MACH__) && !defined(__FreeBSD_kernel__)
 #include "y.tab.h"
 #else
 #include "policy_parse.h"
Index: pkg-ipsec-tools/src/racoon/isakmp.c
===================================================================
--- pkg-ipsec-tools.orig/src/racoon/isakmp.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/racoon/isakmp.c	2014-04-25 23:35:53.703883018 -0700
@@ -122,12 +122,15 @@
 #  endif
 # endif /* __linux__ */
 # if defined(__NetBSD__) || defined(__FreeBSD__) ||	\
-  (defined(__APPLE__) && defined(__MACH__))
+  (defined(__APPLE__) && defined(__MACH__)) || defined(__FreeBSD_kernel__)
+#  define __FAVOR_BSD	/* GNU/kFreeBSD */
 #  include <netinet/in.h>
 #  include <netinet/udp.h>
 #  include <netinet/in_systm.h>
 #  include <netinet/ip.h>
-#  define SOL_UDP IPPROTO_UDP
+#  ifndef SOL_UDP
+#   define SOL_UDP IPPROTO_UDP
+#  endif
 # endif /* __NetBSD__ / __FreeBSD__ */
 
 static int nostate1 __P((struct ph1handle *, vchar_t *));
Index: pkg-ipsec-tools/src/racoon/nattraversal.c
===================================================================
--- pkg-ipsec-tools.orig/src/racoon/nattraversal.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/racoon/nattraversal.c	2014-04-25 23:35:53.703883018 -0700
@@ -38,7 +38,7 @@
 #ifdef __linux__
 #include <linux/udp.h>
 #endif
-#if defined(__NetBSD__) || defined (__FreeBSD__)
+#if defined(__NetBSD__) || defined (__FreeBSD__) || defined(__FreeBSD_kernel__)
 #include <netinet/udp.h>
 #endif
 
Index: pkg-ipsec-tools/src/racoon/pfkey.c
===================================================================
--- pkg-ipsec-tools.orig/src/racoon/pfkey.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/racoon/pfkey.c	2014-04-25 23:35:53.707883042 -0700
@@ -50,7 +50,7 @@
 #  include <linux/udp.h>
 # endif
 # if defined(__NetBSD__) || defined(__FreeBSD__) ||	\
-  (defined(__APPLE__) && defined(__MACH__))
+  (defined(__APPLE__) && defined(__MACH__)) || defined(__FreeBSD_kernel__)
 #  include <netinet/udp.h>
 # endif
 #endif
Index: pkg-ipsec-tools/src/setkey/token.c
===================================================================
--- pkg-ipsec-tools.orig/src/setkey/token.c	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/setkey/token.c	2014-04-25 23:35:53.711883058 -0700
@@ -1051,7 +1051,7 @@
 
 #include "vchar.h"
 #if defined(__NetBSD__) || defined(__FreeBSD__) || defined(__linux__) || \
-(defined(__APPLE__) && defined(__MACH__))
+(defined(__APPLE__) && defined(__MACH__)) || defined(__FreeBSD_kernel__)
 #include "parse.h"
 #else
 #include "y.tab.h"
Index: pkg-ipsec-tools/src/setkey/token.l
===================================================================
--- pkg-ipsec-tools.orig/src/setkey/token.l	2014-04-25 23:35:22.859730075 -0700
+++ pkg-ipsec-tools/src/setkey/token.l	2014-04-25 23:35:53.711883058 -0700
@@ -53,7 +53,7 @@
 
 #include "vchar.h"
 #if defined(__NetBSD__) || defined(__FreeBSD__) || defined(__linux__) || \
-(defined(__APPLE__) && defined(__MACH__))
+(defined(__APPLE__) && defined(__MACH__)) || defined(__FreeBSD_kernel__)
 #include "parse.h"
 #else
 #include "y.tab.h"
